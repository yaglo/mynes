#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct Params
{
    uint count;
};

struct Luma
{
    float y_out[1];
};

struct SourceY
{
    float y_in[1];
};

struct Chroma
{
    float c_out[1];
};

struct Signal
{
    float signal_in[1];
};

constant uint3 gl_WorkGroupSize [[maybe_unused]] = uint3(256u, 1u, 1u);

kernel void main0(constant Params& _19 [[buffer(0)]], device Luma& _34 [[buffer(3)]], device SourceY& _39 [[buffer(2)]], device Chroma& _48 [[buffer(4)]], device Signal& _53 [[buffer(1)]], uint3 gl_GlobalInvocationID [[thread_position_in_grid]])
{
    uint i = gl_GlobalInvocationID.x;
    if (i >= _19.count)
    {
        return;
    }
    _34.y_out[i] = _39.y_in[i];
    _48.c_out[i] = _53.signal_in[i] - _39.y_in[i];
}
